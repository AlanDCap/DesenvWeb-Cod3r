const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2
//operadores binários trabalham com dois operandos

console.log(soma, subtracao, multiplicacao, -divisao, modulo)
                                            //operadores unários trabalham com apenas um operando